VITE_APP_MODE=local

# Backend traffic is off until the Laravel API exists. The app runs entirely on
# the Pinia stores and their mock data. Set to `true` to re-enable requests.
VITE_USE_API=false

# Optional override of apiConfig zcomus_api:
# VITE_API_URL=http://127.0.0.1:8000
